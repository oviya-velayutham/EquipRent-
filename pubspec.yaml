name: equiprent
description: "EquipRent — Equipment Rental Marketplace"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  go_router: ^13.0.0
  google_fonts: ^6.2.1
  image_picker: ^1.0.7
  table_calendar: ^3.1.0
  intl: ^0.20.0
  provider: ^6.1.2
  shared_preferences: ^2.2.3
  firebase_core: ^4.10.0
  firebase_auth: ^6.5.2
  cloud_firestore: ^6.5.0
  firebase_storage: ^13.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
